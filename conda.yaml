channels:
  - conda-forge
  - defaults
  - pytorch

dependencies:
# minimal dependencies for the competition
  - python=3.7
  - scip=7.0.3
  - ecole=0.7.3
  - numpy
  - pyscipopt

# additional conda dependencies go here
  - pytorch
#   - tensorflow
  - scikit-learn

# additional pip dependencies go here
  - pip
  - pip:
    - pandas~=1.3.0
    - comet_ml~=3.14.0
    - ortools~=9.0
    - mip~=1.13.0
    - diskcache~=5.2.1

# Please do not hesitate to send us also a snapshot of your final
# environment to help us troubleshoot potential installation issues:
# conda env export --no-builds
